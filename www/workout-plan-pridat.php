<?php
require_once __DIR__ . '/models/WorkoutPlanModel.php'; require_once __DIR__ . '/models/Database.php';
$db = Database::getInstance()->getConnection(); $model = new WorkoutPlanModel();
$users = $db->query('SELECT * FROM users')->fetchAll(); $goals = $db->query('SELECT * FROM plan_goals')->fetchAll(); $levels = $db->query('SELECT * FROM plan_levels')->fetchAll(); $exercises = $db->query('SELECT * FROM exercises')->fetchAll();
if($_SERVER['REQUEST_METHOD']==='POST'){ $data=['title'=>trim($_POST['title']),'note'=>trim($_POST['note']),'user_id'=>(int)$_POST['user_id'],'goal_id'=>(int)$_POST['goal_id'],'level_id'=>(int)$_POST['level_id'],'is_active'=>isset($_POST['is_active'])?1:0]; $id=$model->insert($data); $model->syncExercises($id, $_POST['exercise_ids']??[]); header('Location: /workout-plan-detail.php?id='.$id); exit; }
?><!doctype html><html><body><?php include __DIR__ . '/includes/nav.php'; ?><h1>Add workout plan</h1><form method="post"><input name="title" required placeholder="Title"><br><textarea name="note"></textarea><br><select name="user_id"><?php foreach($users as $u): ?><option value="<?= $u['id']; ?>"><?= htmlspecialchars($u['name']); ?></option><?php endforeach;?></select><select name="goal_id"><?php foreach($goals as $g): ?><option value="<?= $g['id']; ?>"><?= htmlspecialchars($g['name']); ?></option><?php endforeach;?></select><select name="level_id"><?php foreach($levels as $l): ?><option value="<?= $l['id']; ?>"><?= htmlspecialchars($l['name']); ?></option><?php endforeach;?></select><br><?php foreach($exercises as $e): ?><label><input type="checkbox" name="exercise_ids[]" value="<?= $e['id']; ?>"><?= htmlspecialchars($e['name']); ?></label><?php endforeach; ?><br><label><input type="checkbox" name="is_active" checked>Active</label><button type="submit">Save</button></form></body></html>
